services:
  capstockdashboard:
    image: capstockdashboard
    build:
      context: .
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: development
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - node_modules_data:/app/node_modules

volumes:
  node_modules_data: